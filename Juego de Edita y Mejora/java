document.getElementById('submit-edit').addEventListener('click', function() {
    const userEdit = document.getElementById('user-edit').value;
    // Aquí podrías enviar la edición al servidor o procesarla localmente
    // Por simplicidad, usaremos un texto estático para la retroalimentación
    const feedback = "Aquí están las correcciones sugeridas...";
    document.getElementById('feedback').innerText = feedback;
});
